c = float(input('Digite a temperatura em °C: '))
f = 9*c/5+32
print('A temperatura {}°C pode ser convertida em {}°F'.format(c, f))
#Conversão de temperatura utilizando a forma de cálculo para tal - 9*Celcius/5+32)
